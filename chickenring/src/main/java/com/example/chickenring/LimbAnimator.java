/*
package com.example.chickenring;

public class LimbAnimator {
    private float pos;
    private float prevSpeed;
    private float speed;

    public void updateLimbs(float speed, float multiplier) {
        this.prevSpeed = this.speed;
        this.speed = speed;
        this.pos += this.speed * multiplier;
    }

    public float getSpeed(float tickDelta) {
        return this.prevSpeed + (this.speed - this.prevSpeed) * tickDelta;
    }

    public float getPos(float tickDelta) {
        return this.pos + (this.speed - this.prevSpeed) * tickDelta;
    }
}
*/
